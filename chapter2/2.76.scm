#lang planet neil/sicp

;; 数据导向风格和消息传递风格都对数据和操作进行了隔离
;; 但显示分派的通用性操作不具有这个优势

;; 需要经常添加新类型的系统适合数据导向风格
;; 使用数据导向风格不需要对每个操作都进行修改
;; 即在每个数据包（或函数）中添加（或删除）某种操作

;; 需要经常添加新操作的系统适合消息传递风格
;; 这样不需要在每个操作中都进行修改（增删数据操作）
;; 即在某个操作包(或函数)中添加(或删除)某种数据