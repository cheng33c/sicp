#lang planet neil/sicp

(define (permutations s)
  (if (null? s)
      (list nil)
      ;; 返回当前所有组合的方法
      (flatmap (lambda (x)
                 (map (lambda (p) (cons x p))
                      (permutations (remove x s))))
               s)))

(define (add-to-s n s))