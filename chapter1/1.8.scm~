(define (improve y x)
    (/ (+ (/ x (* y y)) (* 2 y)) 3))

(define (good-enough? guess x)
    (< (abs (- (square guess) x)) 0.001))

(define (my-cube y x)
    (if (good-enough? y x)
        y
        (my-cube (improve y x)
                x)))


(define (cube x)
    (my-cube 1.0 x))