#lang planet neil/sicp

(define (reverse ls)
  (define (iter items answer)
    (if (null? items)
        answer
        (iter items (cons (car items) answer)))))

(define x (list (list 1 2) (list 3 4)))

(reverse x)