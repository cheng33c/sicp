#lang planet neil/sicp

(define (tan-cf x k)
  (define (iter cur res)
    (cond ((= cur 0) res)
          ((= cur k)
           (iter (- cur 1) (/ (n cur) (d cur))))
          (else
           (iter (- cur 1) (/ (n cur)
                              (+ (d cur) res)))))))